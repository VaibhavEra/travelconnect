import { Ionicons } from "@expo/vector-icons";

export const SIZE_ICONS: Record<string, keyof typeof Ionicons.glyphMap> = {
  small: "bag-handle-outline",
  medium: "briefcase-outline",
  large: "cube-outline",
};
